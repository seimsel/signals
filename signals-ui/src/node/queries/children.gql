query Children($nodeId: ID!) {
    node(nodeId: $nodeId) {
        id
        name
        children {
            id
            name
            childCount
            children: null
        }
    }
}
